[project]
name = "pnc-2"
version = "0.1.0"
description = "Prototype collector / technical analyzer"
readme = "Readme.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.2.2",
    "python-telegram-bot>21",
]
